import React from 'React';

import classes from './ViewBookings.module.css';

const ViewBookings = () => {
    return (
        <div>
            <h1>View Bookings</h1>
        </div>
    );
};

export default ViewBookings;
